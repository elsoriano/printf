If more arguments that % --> shows only arguments with %. 
If more % than arguments --> shows arguments and shows info of next va_next memory slot --> undefined
If %c pass int asccii. If no char print space. 
If conversion does not exist --> segfault
If no percentage --> shows text
If no string --> shows nothing
If %c and string argument --> undefined

